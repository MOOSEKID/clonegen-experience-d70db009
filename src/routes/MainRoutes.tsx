
/**
 * MainRoutes.tsx has been deprecated.
 * All main routes have been moved directly into App.tsx for better route organization
 * and to avoid nesting components in a way that React Router doesn't support.
 * 
 * This file is kept for reference but is no longer in use.
 */

import { Route } from "react-router-dom";

// This component is no longer used
const MainRoutes = () => null;

export default MainRoutes;
